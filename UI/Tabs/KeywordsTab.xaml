<UserControl x:Class="WhatsAppSimHubPlugin.UI.Tabs.KeywordsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:styles="clr-namespace:SimHub.Plugins.Styles;assembly=SimHub.Plugins">

    <StackPanel Margin="20">

        <!-- Urgent Keywords -->
        <TextBlock Text="Urgent Keywords"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="#00BCD4"
                   Margin="0,0,0,5"/>
        <Border Height="1" Background="#00BCD4" Margin="0,0,0,15"/>

        <!-- Add New Keyword -->
        <StackPanel Margin="0,0,0,20">
            <TextBlock Text="+ Add New Keyword"
                       FontWeight="Bold"
                       FontSize="14"
                       Margin="0,0,0,12"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" x:Name="NewKeyword"
                         Margin="0,0,10,0"
                         Height="36"/>

                <styles:SHButtonPrimary Grid.Column="1" x:Name="AddKeywordButton"
                        Content="➕ Add"
                        Width="100" Height="36"
                        Background="#0E7A0D"
                        Foreground="White"/>
            </Grid>
        </StackPanel>

        <!-- Keywords List -->
        <ListBox x:Name="KeywordsListBox"
                 MinHeight="150"
                 MaxHeight="300"
                 Background="#252526"
                 Padding="5"
                 Margin="0,0,0,20">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="#2D2D30"
                            CornerRadius="4"
                            Padding="12,8"
                            Margin="0,2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding}"
                                       VerticalAlignment="Center"
                                       FontSize="13"
                                       Foreground="White"/>

                            <styles:SHButtonPrimary Grid.Column="1" x:Name="RemoveKeywordButton"
                                    Content="🗑️ Remove"
                                    Width="110" Height="28"
                                    Background="#C42B1C"
                                    Foreground="White"
                                    FontSize="11"
                                    Tag="{Binding}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Info -->
        <Border Background="#1E3A5F" Padding="12" CornerRadius="6">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="ℹ️" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Top" Foreground="White"/>
                <TextBlock TextWrapping="Wrap"
                           Foreground="White"
                           FontSize="12"
                           LineHeight="18">
                    Messages containing these keywords will be marked as URGENT.
                    <LineBreak/>
                    Urgent messages have higher priority and repeat until read.
                </TextBlock>
            </StackPanel>
        </Border>

    </StackPanel>
</UserControl>
