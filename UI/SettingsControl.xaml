<UserControl x:Class="WhatsAppSimHubPlugin.UI.SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:styles="clr-namespace:SimHub.Plugins.Styles;assembly=SimHub.Plugins"
             xmlns:ui="clr-namespace:SimHub.Plugins.UI;assembly=SimHub.Plugins"
             xmlns:tabs="clr-namespace:WhatsAppSimHubPlugin.UI.Tabs"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">



    <Grid Margin="10" >
        <!-- Toast Notification - Above Tabs -->
        <Border x:Name="ToastNotification"
                Background="#2D2D30"
                BorderBrush="#007ACC"
                BorderThickness="1"
                CornerRadius="5"
                Padding="15,10"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Margin="0,-50,0,0"
                Visibility="Collapsed"
                MaxWidth="400"
                Panel.ZIndex="999">
            <Border.Effect>
                <DropShadowEffect Color="#007ACC" Opacity="0.6" BlurRadius="15" ShadowDepth="0"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="ToastIcon"
                           Text="ℹ️"
                           FontSize="20"
                           Margin="0,0,10,0"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="ToastMessage"
                           Text="Notification"
                           FontSize="14"
                           Foreground="White"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <styles:SHTabControl>
            <!-- TAB 1: CONNECTION -->
            <styles:SHTabItem Header="🔌 Connection">
                <tabs:ConnectionTab x:Name="ConnectionTab"/>
            </styles:SHTabItem>

            <!-- TAB 2: CONTACTS -->
            <styles:SHTabItem Header="👥 Contacts">
                <tabs:ContactsTab x:Name="ContactsTab"/>
            </styles:SHTabItem>

            <!-- TAB 3: KEYWORDS -->
            <styles:SHTabItem Header="🚨 Keywords">
                <tabs:KeywordsTab x:Name="KeywordsTab"/>
            </styles:SHTabItem>

            <!-- TAB 4: DISPLAY -->
            <styles:SHTabItem Header="🖥️ Display">
                <tabs:DisplayTab x:Name="DisplayTab"/>
            </styles:SHTabItem>

            <!-- TAB 5: QUEUE -->
            <styles:SHTabItem Header="⏱️ Queue">
                <tabs:QueueTab x:Name="QueueTab"/>
            </styles:SHTabItem>

            <!-- TAB 6: QUICK REPLIES -->
            <styles:SHTabItem Header="⚡ Quick Replies">
                <tabs:QuickRepliesTab x:Name="QuickRepliesTab"/>
            </styles:SHTabItem>
        </styles:SHTabControl>
    </Grid>
</UserControl>
